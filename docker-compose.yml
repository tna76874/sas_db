services:
  sas-qr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sas_qr_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
