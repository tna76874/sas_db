name: Build Cross-Platform Binaries

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
        include:
          - os: windows-latest
            artifact_name: sas_qr.exe
            file_name: run.exe
          - os: ubuntu-latest
            artifact_name: sas_qr.AppImage
            file_name: run.bin
        
    steps:
    - name: Checkout external repository
      uses: actions/checkout@v4
      with:
        repository: ${{ secrets.BUILD_REPO }}
        github-server-url: ${{ secrets.REPO_HOST }}
        token: ${{ secrets.REPO_TOKEN }}
        ref: 'main'

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip'

    - name: Install Base Dependencies
      run: |
        python -m pip install --upgrade pip
        python -c "import os; os.system('pip install -r requirements.txt') if os.path.exists('requirements.txt') else None"
        python -m pip install nuitka zstandard

    - name: Build Windows EXE
      if: ${{ matrix.os == 'windows-latest' }}
      run: |
        ./build.bat

    - name: Build Linux Binary
      if: ${{ matrix.os == 'ubuntu-latest' }}
      run: |
        chmod +x build.sh
        ./build.sh

    # Hier laden wir die Binaries temporär hoch
    - name: Upload temporary artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: ${{ matrix.file_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Get current date
      id: date
      run: echo "date=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT

    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        merge-multiple: true 

    # Erstellt den Date-Release und lädt BEIDE Dateien auf einmal hoch
    - name: Create Date Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: release-${{ steps.date.outputs.date }}
        name: Release vom ${{ steps.date.outputs.date }}
        body: "Archiv-Release vom ${{ steps.date.outputs.date }}."
        files: |
          sas_qr.exe
          sas_qr.AppImage

    # Aktualisiert den Latest-Release mit BEIDEN Dateien auf einmal
    - name: Update Latest Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: latest
        name: Latest Release
        body: "Die aktuellsten Binaries.."
        files: |
          sas_qr.exe
          sas_qr.AppImage